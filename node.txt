1.异步async
await 会返回一个 promise对象 
const a = await new Promise((resolve, reject) => {
  setTimeout(function () {
    new Date().getTime()
    resolve('a') // 返回的值
  }, 1000)
})

2.koa中间件
中间件： 请求和相应都执行
环形 next执行下一个中间件
实现一个中间件